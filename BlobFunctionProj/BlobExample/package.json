{
  "name": "blobexample",
  "version": "1.0.0",
  "description": "The `BlobTrigger` makes it incredibly easy to react to new Blobs inside of Azure Blob Storage. This sample demonstrates a simple use case of processing data from a given Blob using JavaScript.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "deeshant",
  "license": "ISC",
  "dependencies": {
    "@azure/storage-blob": "^12.10.0",
    "kafkajs": "^2.1.0",
    "openpgp": "^5.3.0"
  }
}
